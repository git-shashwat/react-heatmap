export default (param) => {
    if (param === 1) {
        return 'Potenial'
    } else if (param === 2) {
        return 'Possible'
    } else if (param === 3) {
        return 'Probbable'
    } else if (param === 4) {
        return 'Buisness as Usual'
    } else {
        return ''
    }
}